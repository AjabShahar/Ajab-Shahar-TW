'use strict';

describe('Ajab-Shahar Introduction', function () {
  var route;

  beforeEach(function () {
    module('ajabShaharApp');
    module('trustUrl');
    inject(function ($route) {
      route = $route;
    });
  });


});