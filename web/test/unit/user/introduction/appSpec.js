'use strict';

describe('Ajab-Shahar Introduction', function () {
    var route;

    beforeEach(function () {
        module('introductionApp');
        module('trustUrl');
        inject(function ($route) {
            route = $route;
        });
    });


});
